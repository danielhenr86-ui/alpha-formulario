{
  "name": "meton-diagnostico",
  "version": "1.0.0",
  "private": true,
  "description": "Diagnóstico financeiro e captação de leads da MetOn",
  "scripts": {
    "check": "node --check api/lead.js && node --test",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
